<div style="margin-left: 3px;">
<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, size: "172x5", :maxlength => 125 %>
  </div>
  <br>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, size: "150x15" %>
  </div>
  <!-- div class="field">
    <!-- %= f.label :user_id %><br>
    <!-- %= f.number_field :user_id %>
  </div -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
